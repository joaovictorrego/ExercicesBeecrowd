#include <stdio.h>
#include <stdlib.h>

int fib(int n){
  int *fib_chamadas;
  int i;
  int r = 0;
  fib_chamadas = (int *) malloc(sizeof(int) * (n + 1));
  fib_chamadas[0] = 0;
  fib_chamadas[1] = 1;

  for( i = 2; i < n; i++){
    r = 
  }
  
}

int main(){
  
  int n;
  int b;
  int r;
  int fi;
  int casos = 0;
  
  scanf("%d %d", &n, &b);
  while(n >=  0 && b >=  0){
    casos++;
    fi = fib(n);
    r = fi % b;
    printf("Case %d: %d %d %d\n", casos, n, b, r);
    scanf("%d %d", &n, &b);
    free(numerosChamadas);
  }
  return 0;
}
